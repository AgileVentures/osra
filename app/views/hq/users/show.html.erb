<dl class="dl-horizontal">
  <dt>User Name</dt>
  <dd><%= @user.user_name %></dd>
</dl>
<dl class="dl-horizontal">
  <dt>Email Address</dt>
  <dd><%= @user.email %></dd>
</dl>
<div class="active_sponsors">
  <%= pluralize(@user.active_sponsors.count.to_s, 'Active Sponsor') %>
  <% if @user.active_sponsors.count > 0 %>
    <div class="active_sponsors_index">
      <%= render partial: 'hq/sponsors/index.html.haml', locals: {sponsors: @user.active_sponsors} %>
    </div>
  <% end %>
</div>
<div class="inactive_sponsors">
  <%= pluralize(@user.inactive_sponsors.count.to_s, 'Inactive Sponsor') %>
  <% if @user.inactive_sponsors.count > 0 %>
    <div class="inactive_sponsors_index">
      <%= render partial: 'hq/sponsors/index.html.haml', locals: {sponsors: @user.inactive_sponsors} %>
    </div>
  <% end %>
</div>
