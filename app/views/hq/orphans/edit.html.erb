<%= render partial: "layouts/content_title.erb",
           locals: {title: "Edit Orphan #{@orphan.name}, OSRA Number #{@orphan.osra_num}"} %>
<%= render partial: 'form', locals: { orphan: @orphan,
                                      statuses: @statuses,
                                      sponsorship_statuses: @sponsorship_statuses,
                                      provinces: @provinces }%>
