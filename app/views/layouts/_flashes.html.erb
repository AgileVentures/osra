<% unless flash.empty? %>
  <div class="flashes">
    <% flash.each do |type, message| %>
      <div class="<%= flash_htmlclass_for type %>"><%= message.to_s %></div>
    <% end %>
  </div>
<% end %>
